# grocery-browser sidecar runtime dependencies (spec 11)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Undetected CDP-based Chrome driver (Python-only) — drives the real browser.
# https://github.com/cdpdriver/zendriver
zendriver==0.5.1
